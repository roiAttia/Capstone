<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="roiattia.com.capstone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTop"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.calendar.CalendarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.newjob.NewJobActivity" />
        <activity android:name=".ui.joblist.JobsListActivity" />
        <activity android:name=".ui.finances.FinancesActivity">

            <!-- <intent-filter> -->
            <!-- <action android:categoryName="android.intent.action.MAIN" /> -->


            <!-- <category android:categoryName="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.newexpense.ExpenseActivity" />
        <activity android:name=".ui.newexpense.ExpenseActivity"></activity>
    </application>

</manifest>